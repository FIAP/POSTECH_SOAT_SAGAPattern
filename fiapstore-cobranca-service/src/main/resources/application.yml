logging:
    level:
        br:
            com: DEBUG
queue1:
    name: pedidos
queue2:
    name: pagamentos_pendentes
queue3:
    name: pagamentos_confirmados
queue4:
    name: entregas_confirmadas
server:
    port: 8082
spring:
    application:
        name: Cobranca Service

    datasource:
        driverClassName: org.h2.Driver
        username: ola
        password: mundo
        url: jdbc:h2:file:D:/_dev_ssd/databases_demo/cobrancaDB;mode=PostgreSQL;AUTO_SERVER=TRUE

    h2:
        console:
            enabled: true
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        defer-datasource-initialization: true
        hibernate:
            ddl-auto: update
        show-sql: true

    rabbitmq:
        host: localhost
        password: guest
        port: 5672
        username: guest

springdoc:
    swagger-ui:
        path: /doc
